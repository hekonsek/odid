# Configuration

Odid is expected to be executed from audited project directory. Odid configuration lives in `.odid` directory in current project.

Directory should be created by application only if it is needed.
